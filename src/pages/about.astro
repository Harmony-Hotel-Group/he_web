---
//pages/about.astro

// export const prerender = false;

import { Translations } from "@/i18n/translation.ts";
import { logger } from "@/services/logger.ts";

interface Props {
	lang: string;
}

const { lang = "es" } = Astro.props;

const log = logger("about");

import UnderConstructionPage from "@/components/pages/UnderConstructionPage.astro";

const t = Translations(lang);
log.info(`Rendering about page with language: ${lang}`);
---

<UnderConstructionPage
        titleHead={t("nav.about")}
        title={t("underConstruction.about.title")}
        message={t("underConstruction.about.message")}
        btnBackLabel={t("underConstruction.back")}
        lang={lang}
/>
