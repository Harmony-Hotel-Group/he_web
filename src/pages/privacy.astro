---
//pages/privacy.astro

// export const prerender = false;

import { Translations } from "@/i18n/translation.ts";
import { logger } from "@/services/logger.ts";

interface Props {
	lang: string;
}

const { lang = "es" } = Astro.props;

const log = logger("privacy");

import UnderConstructionPage from "@/components/pages/UnderConstructionPage.astro";

const t = Translations(lang);
log.info(`Rendering privacy page with language: ${lang}`);
---

<UnderConstructionPage
        titleHead={t("nav.privacy")}
        title={t("underConstruction.privacy.title")}
        message={t("underConstruction.privacy.message")}
        btnBackLabel={t("underConstruction.back")}
        lang={lang}
/>
