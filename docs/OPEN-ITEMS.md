# Open Items
- Elegir UNA sola fase del Plan Unificado para la próxima iteración.
- Accesibilidad: pendiente reparar violaciones cuando se permita modificar `src/**`.
- E2E: pendiente resolver dependencias de navegadores en CI para WebKit/Firefox.
- E2E Chromium: documentar fallos persistentes antes de acumular deuda sin trazar.
- E2E Chromium (documentado): los selectores actuales en E2E no sostienen el paso a 33/33; se mantiene Fase 5 como foco mientras se registra deuda explícita sin reabrir Fase 4.
- E2E Chromium: pendiente desbloquear selector de flatpickr en `booking.spec.ts` y `visual-autonomous.spec.ts`, ajustar campos de edad/vehículo, confirmar header Cache-Control y, si aplica, revisar `api-ssr.spec.ts` e `i18n-currency.spec.ts`. No se mezclan cambios de producción hasta la próxima ventana.
