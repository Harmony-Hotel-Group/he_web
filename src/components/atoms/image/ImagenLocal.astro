---
import type { ImageMetadata } from "astro";
import ImagenBase from "./ImagenBase.astro";

interface Props {
	src: string | ImageMetadata;
	alt: string;
	[rest: string]: unknown;
}

const { src, alt, ...rest } = Astro.props as Props;
---

<ImagenBase src={src} alt={alt} {...rest} />
