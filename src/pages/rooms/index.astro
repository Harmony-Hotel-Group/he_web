---
import { useI18n } from '@/i18n/useI18n';
import HotelLayout from '@/layouts/HotelLayout.astro';
import RoomCard from '@/components/hotel/RoomCard.jsx';

const { rooms } = await import('@/data/rooms.json');
const { t } = useI18n();

const title = t('rooms.title');
const description = t('rooms.description');
---

<HotelLayout {title} {description}>
  <!-- Page Header -->
  <section class="bg-gray-50 py-16">
    <div class="container mx-auto px-4 text-center">
      <h1 class="text-4xl md:text-5xl font-bold mb-4">
        {t('rooms.title')}
      </h1>
      <p class="text-lg text-gray-700 max-w-2xl mx-auto">
        {t('rooms.subtitle')}
      </p>
    </div>
  </section>

  <!-- Rooms Grid -->
  <section class="py-16">
    <div class="container mx-auto px-4">
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {rooms.map(room => (
          <RoomCard room={room} currency="USD" client:load />
        ))}
      </div>
    </div>
  </section>

  <!-- Additional Information Section -->
  <section class="py-16 bg-gray-50">
    <div class="container mx-auto px-4">
      <div class="max-w-4xl mx-auto text-center">
        <h2 class="text-3xl font-bold mb-8">
          {t('rooms.amenities')}
        </h2>

        <div class="grid grid-cols-2 md:grid-cols-4 gap-8">
          <div class="text-center">
            <div class="text-4xl mb-2">📶</div>
            <h3 class="font-semibold">{t('amenities.wifi')}</h3>
          </div>
          <div class="text-center">
            <div class="text-4xl mb-2">🏊‍♂️</div>
            <h3 class="font-semibold">{t('amenities.pool')}</h3>
          </div>
          <div class="text-center">
            <div class="text-4xl mb-2">🍽️</div>
            <h3 class="font-semibold">{t('amenities.restaurant')}</h3>
          </div>
          <div class="text-center">
            <div class="text-4xl mb-2">🅿️</div>
            <h3 class="font-semibold">{t('amenities.parking')}</h3>
          </div>
        </div>
      </div>
    </div>
  </section>
</HotelLayout>