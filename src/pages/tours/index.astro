---
import UnderConstruction from "@/components/pages/UnderConstruction.astro";
import BaseLayout from "@/layouts/BaseLayout.astro";
import type { SiteConfig } from "@/types/config";
import { api } from "@/services/api.ts";

export const prerender = false;

const lang = "es";
const config = (await api.fetch("config", { params: { lang } })) as SiteConfig;
---

<BaseLayout titleKey="nav.tours" lang={lang} config={config}>
    <UnderConstruction 
        title="Tours" 
        message="Estamos recopilando los tours más habituales para ayudarte. Pronto estará disponible."
        backPath="/"
        backLabel="Volver al Inicio"
    />
</BaseLayout>
