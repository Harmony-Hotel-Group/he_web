---
interface Props {
	currentLang?: string;
}

const { currentLang = "es" } = Astro.props;
const currentYear = new Date().getFullYear();
const startYear = 2024;

// Navegación principal
const mainLinks = [
	{ href: `/${currentLang}`, label: "Inicio" },
	{ href: `/${currentLang}/rooms`, label: "Habitaciones" },
	{
		href: `/${currentLang}/destinations`,
		label: "Destinos",
	},
	{ href: `/${currentLang}/tours`, label: "Tours" },
	{
		href: `/${currentLang}/gastronomy`,
		label: "Gastronomía",
	},
];

// Páginas de interés
const interestLinks = [
	{
		href: `/${currentLang}/about`,
		label: "Sobre Nosotros",
	},
	{ href: `/${currentLang}/gallery`, label: "Galería" },
	{ href: `/${currentLang}/contact`, label: "Contacto" },
	{ href: `/${currentLang}/booking`, label: "Reservar" },
];

// Información legal
const legalLinks = [
	{
		href: `/${currentLang}/privacy`,
		label: "Política de Privacidad",
	},
	{
		href: `/${currentLang}/terms`,
		label: "Términos y Condiciones",
	},
	{
		href: `/${currentLang}/cancellation`,
		label: "Política de Cancelación",
	},
	{
		href: `/${currentLang}/faq`,
		label: "Preguntas Frecuentes",
	},
];

// Redes sociales (ajusta según tu config.json)
const socialLinks = [
	{
		href: "https://facebook.com/hotelensueños",
		label: "Facebook",
		icon: "facebook",
	},
	{
		href: "https://instagram.com/hotelensueños",
		label: "Instagram",
		icon: "instagram",
	},
	{
		href: "https://twitter.com/hotelensueños",
		label: "Twitter",
		icon: "twitter",
	},
	{
		href: "https://tripadvisor.com",
		label: "TripAdvisor",
		icon: "tripadvisor",
	},
];
---

<footer class="bg-linear-to-b from-gray-900 to-gray-950 text-gray-300">
    <!-- Contenido principal del footer -->
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12 lg:py-16">
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8 lg:gap-12">

            <!-- Columna 1: Sobre el Hotel -->
            <div class="space-y-4">
                <h3 class="text-white text-lg font-semibold mb-4 border-b border-gray-700 pb-2">
                    Hotel Ensueños
                </h3>
                <p class="text-sm text-gray-400 leading-relaxed">
                    Hotel boutique en el corazón de Cuenca, Ecuador. Experiencias únicas en el centro histórico.
                </p>
                <div class="space-y-2 text-sm">
                    <p class="flex items-start gap-2">
                        <svg class="w-5 h-5 text-blue-400 shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
                        </svg>
                        <span class="text-gray-400">Centro Histórico, Cuenca, Ecuador</span>
                    </p>
                    <p class="flex items-center gap-2">
                        <svg class="w-5 h-5 text-green-400 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
                        </svg>
                        <a href="tel:+593999999999" class="text-gray-400 hover:text-white transition-colors">
                            +593 99 999 9999
                        </a>
                    </p>
                    <p class="flex items-center gap-2">
                        <svg class="w-5 h-5 text-red-400 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
                        </svg>
                        <a href="mailto:info@hotelensueños.com" class="text-gray-400 hover:text-white transition-colors">
                            info@hotelensueños.com
                        </a>
                    </p>
                </div>
            </div>

            <!-- Columna 2: Navegación Principal -->
            <div class="space-y-4">
                <h3 class="text-white text-lg font-semibold mb-4 border-b border-gray-700 pb-2">
                    Navegación
                </h3>
                <ul class="space-y-2">
                    {mainLinks.map(link => (
                            <li>
                                <a
                                        href={link.href}
                                        class="text-sm text-gray-400 hover:text-white hover:translate-x-1 inline-block transition-all duration-200"
                                >
                                    {link.label}
                                </a>
                            </li>
                    ))}
                </ul>
            </div>

            <!-- Columna 3: Páginas de Interés -->
            <div class="space-y-4">
                <h3 class="text-white text-lg font-semibold mb-4 border-b border-gray-700 pb-2">
                    Información
                </h3>
                <ul class="space-y-2">
                    {interestLinks.map(link => (
                            <li>
                                <a
                                        href={link.href}
                                        class="text-sm text-gray-400 hover:text-white hover:translate-x-1 inline-block transition-all duration-200"
                                >
                                    {link.label}
                                </a>
                            </li>
                    ))}
                </ul>
            </div>

            <!-- Columna 4: Legal & Redes Sociales -->
            <div class="space-y-6">
                <div class="space-y-4">
                    <h3 class="text-white text-lg font-semibold mb-4 border-b border-gray-700 pb-2">
                        Legal
                    </h3>
                    <ul class="space-y-2">
                        {legalLinks.map(link => (
                                <li>
                                    <a
                                            href={link.href}
                                            class="text-sm text-gray-400 hover:text-white hover:translate-x-1 inline-block transition-all duration-200"
                                    >
                                        {link.label}
                                    </a>
                                </li>
                        ))}
                    </ul>
                </div>

                <!-- Redes Sociales -->
                <div class="space-y-4">
                    <h3 class="text-white text-lg font-semibold">
                        Síguenos
                    </h3>
                    <div class="flex gap-3">
                        {socialLinks.map(social => (
                                <a
                                        href={social.href}
                                        target="_blank"
                                        rel="noopener noreferrer"
                                        class="w-10 h-10 bg-gray-800 rounded-full flex items-center justify-center hover:bg-blue-600 transition-all duration-300 hover:scale-110"
                                        aria-label={social.label}
                                >
                                <span class="text-white text-lg">
                                    {social.icon === 'facebook' && '📘'}
                                    {social.icon === 'instagram' && '📷'}
                                    {social.icon === 'twitter' && '🐦'}
                                    {social.icon === 'tripadvisor' && '🦉'}
                                </span>
                                </a>
                        ))}
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Separador -->
    <div class="border-t border-gray-800"></div>

    <!-- Copyright y créditos -->
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
        <div class="flex flex-col md:flex-row justify-between items-center gap-4 text-sm text-gray-500">
            <!-- Derechos reservados -->
            <div class="text-center md:text-left">
                <p class="font-medium text-gray-400">
                    &copy; {startYear} - {currentYear} Harmony Hotel Group
                </p>
                <p class="text-xs mt-1">
                    Todos los derechos reservados
                </p>
            </div>

            <!-- Créditos de desarrollo -->
            <div class="text-center md:text-right">
                <p class="text-xs text-gray-500">
                    Desarrollado por
                </p>
                <a
                        href="https://innotec-itsc.com"
                        target="_blank"
                        rel="noopener noreferrer"
                        class="text-sm font-medium text-gray-400 hover:text-blue-400 transition-colors inline-flex items-center gap-1 group"
                >
                    <span class="group-hover:underline">Innotec ITSC</span>
                    <svg class="w-3 h-3 group-hover:translate-x-0.5 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3"/>
                    </svg>
                </a>
                <p class="text-xs text-gray-600 mt-0.5">
                    Innovation Technology Software Company
                </p>
            </div>
        </div>
    </div>
</footer>