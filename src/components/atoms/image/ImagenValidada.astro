---
import ImagenBase from "./ImagenBase.astro";
import { validateImageSource } from "@/adapters/image/image.adapter";

const { src, alt } = Astro.props;
const safeSrc = validateImageSource(src);
---

<ImagenBase src={safeSrc} alt={alt} />
