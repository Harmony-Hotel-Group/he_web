---
//pages/contact.astro

// export const prerender = false;

import { Translations } from "@/i18n/translation.ts";
import { logger } from "@/services/logger.ts";

interface Props {
	lang: string;
}

const { lang = "es" } = Astro.props;

const log = logger("contact");

import UnderConstructionPage from "@/components/pages/UnderConstructionPage.astro";

const t = Translations(lang);
log.info(`Rendering contact page with language: ${lang}`);
---

<UnderConstructionPage
        titleHead={t("nav.contact")}
        title={t("underConstruction.contact.title")}
        message={t("underConstruction.contact.message")}
        btnBackLabel={t("underConstruction.back")}
        lang={lang}
/>
