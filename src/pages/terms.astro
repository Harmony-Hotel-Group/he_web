---
import BaseLayout from "@/layouts/BaseLayout.astro";
import UnderConstruction from "@/components/pages/UnderConstruction.astro";
import type { SiteConfig } from "@/types/config";
import { api } from "@/services/api.ts";

export const prerender = false;

const lang = "es";
const config = (await api.fetch("config", { params: { lang } })) as SiteConfig;
---

<BaseLayout titleKey="footer.terms" lang={lang} config={config}>
    <UnderConstruction 
        title="Términos y Condiciones" 
        message="Estamos revisando nuestros términos legales para garantizar claridad y transparencia."
        backPath="/"
        backLabel="Volver al Inicio"
    />
</BaseLayout>
