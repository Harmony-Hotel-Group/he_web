---
import ImagenBase from "./ImagenBase.astro";
import { validateImageSource } from "@/utils/image";

const { src, alt } = Astro.props;
const safeSrc = await validateImageSource(src);
---

<ImagenBase src={safeSrc} alt={alt} />
