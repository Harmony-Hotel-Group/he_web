---
// src/components/organisms/booking/BookingForm.astro
import DatePickerField from "../../molecules/forms/DatePickerField.astro";
import Button from "../../atoms/Button.astro";

import { Translations } from "~/i18n/translation.ts";
import { server } from '~/actions/index.ts';

interface Props {
    lang: string;
    config: any;
}

const {lang, config} = Astro.props;

const t = Translations(lang);

function btnClic(date: string) {
    console.log(date);
}
---

<form id="booking-form" action={server.booking} method="post" class="bg-white/20 p-6 rounded-xl shadow-lg space-y-6">
    <DatePickerField lblKey="booking.dateRangePicker" lang={lang}/>
    <div class="text-center pt-4">
        <Button type="submit" lblKey="booking.search" lang={lang} variant="accent" className="w-full md:w-auto px-12"/>
    </div>
</form>