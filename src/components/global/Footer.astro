---
import { useI18n } from '@/i18n/useI18n';
import hotel from '@/data/hotel.json';

const { siteConfig } = await import('@/data/siteConfig.json');
const { t } = useI18n();
---

<footer class="bg-gray-800 text-white py-12">
  <div class="container mx-auto px-4">
    <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
      <!-- Hotel Info -->
      <div class="col-span-1 md:col-span-2">
        <h3 class="text-xl font-bold mb-4">{siteConfig.siteName || 'Hotel Ensueños'}</h3>
        <p class="text-gray-300 mb-4">
          {t('footer.description')}
        </p>
        <div class="space-y-2">
          <p class="text-gray-300">
            📍 {hotel.location?.address || 'Centro Histórico, Cuenca, Ecuador'}
          </p>
          <p class="text-gray-300">
            📞 {hotel.contact?.phone || '+593999999999'}
          </p>
          <p class="text-gray-300">
            ✉️ {hotel.contact?.email || 'info@hotelensuenos.com'}
          </p>
        </div>
      </div>

      <!-- Quick Links -->
      <div>
        <h4 class="text-lg font-semibold mb-4">{t('footer.quickLinks')}</h4>
        <ul class="space-y-2">
          <li>
            <a href="/" class="text-gray-300 hover:text-white transition-colors">
              {t('nav.home')}
            </a>
          </li>
          <li>
            <a href="/rooms" class="text-gray-300 hover:text-white transition-colors">
              {t('nav.rooms')}
            </a>
          </li>
          <li>
            <a href="/tourism" class="text-gray-300 hover:text-white transition-colors">
              {t('nav.tourism')}
            </a>
          </li>
          <li>
            <a href="/gastronomy" class="text-gray-300 hover:text-white transition-colors">
              {t('nav.gastronomy')}
            </a>
          </li>
          <li>
            <a href="/contact" class="text-gray-300 hover:text-white transition-colors">
              {t('nav.contact')}
            </a>
          </li>
        </ul>
      </div>

      <!-- Contact & Social -->
      <div>
        <h4 class="text-lg font-semibold mb-4">{t('footer.contact')}</h4>
        <div class="space-y-2 mb-4">
          <p class="text-gray-300">
            {t('footer.checkIn')}: {hotel.checkInTime || '15:00'}
          </p>
          <p class="text-gray-300">
            {t('footer.checkOut')}: {hotel.checkOutTime || '12:00'}
          </p>
        </div>

        <div>
          <h5 class="text-sm font-semibold mb-2">{t('footer.followUs')}</h5>
          <div class="flex space-x-4">
            <a href="#" class="text-gray-300 hover:text-white transition-colors">
              <span class="sr-only">Facebook</span>
              📘
            </a>
            <a href="#" class="text-gray-300 hover:text-white transition-colors">
              <span class="sr-only">Instagram</span>
              📷
            </a>
            <a href="#" class="text-gray-300 hover:text-white transition-colors">
              <span class="sr-only">Twitter</span>
              🐦
            </a>
            <a href={`https://wa.me/${siteConfig.whatsappContact?.replace('+', '')}`} class="text-gray-300 hover:text-white transition-colors">
              <span class="sr-only">WhatsApp</span>
              💬
            </a>
          </div>
        </div>
      </div>
    </div>

    <!-- Bottom Bar -->
    <div class="border-t border-gray-700 mt-8 pt-8 text-center">
      <p class="text-gray-400">
        © {new Date().getFullYear()} {siteConfig.siteName || 'Hotel Ensueños'}. {t('footer.rights')}
      </p>
    </div>
  </div>
</footer>