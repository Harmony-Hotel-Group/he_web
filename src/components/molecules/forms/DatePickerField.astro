---
// src/components/molecules/forms/DatePickerField.astro
import DateRangePicker from '../../atoms/DateRangePicker.astro';
import Label from '../../atoms/Label.astro';

interface Props {
    fieldId?: string;
    name: string;
    lblKey: string;
    lang: string;
    className?: string;
}

const {fieldId = 'date-range-picker', name, lblKey, lang, className} = Astro.props;
console.log("DatePickerField.astro Completely loaded");
---

<div class:list={["flex flex-col", className]}>
    <Label
            forId={fieldId}
            lblKey={lblKey}
            lang={lang}
            className="font-bold mb-2"
    />

    <DateRangePicker
            forId={fieldId}
            name={name}
            lang={lang}
    />
</div>
