---
//pages/terms.astro

// export const prerender = false;

import { Translations } from "@/i18n/translation.ts";
import { logger } from "@/services/logger.ts";

interface Props {
	lang: string;
}

const { lang = "es" } = Astro.props;

const log = logger("terms");

import UnderConstructionPage from "@/components/pages/UnderConstructionPage.astro";

const t = Translations(lang);
log.info(`Rendering terms page with language: ${lang}`);
---

<UnderConstructionPage
        titleHead={t("nav.terms")}
        title={t("underConstruction.terms.title")}
        message={t("underConstruction.terms.message")}
        btnBackLabel={t("underConstruction.back")}
        lang={lang}
/>
